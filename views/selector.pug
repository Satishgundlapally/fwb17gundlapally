extends layout

block content
  h1= title
     - var image_names=["e1.jpg","e2.jpg","e3.jpg","e4.jpg","e5.jpg"]
        - for(let i=0; i<3; i++) {
          - var c= Math.floor(Math.random()*5)
            img(src=`/images/${image_names[c]}` alt=`${c}}` width='300px' height='300px')
        - }
  
        
 
